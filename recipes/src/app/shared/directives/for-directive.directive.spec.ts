import { ForDirectiveDirective } from './for-directive.directive';

describe('ForDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ForDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
