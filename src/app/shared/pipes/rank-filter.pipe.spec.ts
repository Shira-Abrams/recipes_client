import { RankFilterPipe } from './rank-filter.pipe';

describe('RankFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RankFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
