import { CategorypipesPipe } from './categorypipes.pipe';

describe('CategorypipesPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorypipesPipe();
    expect(pipe).toBeTruthy();
  });
});
